# Partners Landing - Страница авторизации для IMS Frontend

Современная страница авторизации с интеграцией IMS Frontend API, созданная с использованием React и Vite.

## 🚀 Возможности

- **Интеграция с IMS Frontend** - Полная интеграция с API авторизации
- **Современный дизайн** - Красивый градиентный фон и стеклянный эффект
- **Адаптивность** - Отлично работает на всех устройствах
- **Валидация форм** - Проверка email и пароля в реальном времени
- **Обработка ошибок** - Детальная обработка различных типов ошибок API
- **Автоматический редирект** - После авторизации перенаправление на dashboard IMS Frontend
- **Безопасность** - Безопасное хранение токенов и защита от XSS

## 🛠 Технологии

- **React 18** - Современная библиотека для UI
- **Vite** - Быстрый сборщик
- **IMS Frontend API** - Интеграция с существующим API
- **CSS3** - Современные стили с градиентами и анимациями

## 📦 Установка

1. Установите зависимости:
```bash
npm install
```

2. Настройте переменные среды для нужного окружения:
```bash
# Для разработки
cp .env.example .env.development

# Для продакшена  
cp .env.example .env.production
```

3. Отредактируйте соответствующий файл с вашими настройками API

4. Запустите проект в режиме разработки:
```bash
npm run dev
```

5. Откройте браузер по адресу `http://localhost:3000`

## ⚙️ Конфигурация

### Переменные среды

Проект использует разделение переменных среды по окружениям:

- **`.env.development`** - для разработки
- **`.env.production`** - для продакшена

Создайте нужный файл на основе `.env.example`:

**Development (.env.development):**
```env
VITE_API_URL=https://app-ims-api-test-neu.azurewebsites.net
VITE_NODE_ENV=development
# ... остальные переменные
```

**Production (.env.production):**
```env
VITE_API_URL=https://app-ims-api-prod-neu-eqhkdxa0f9b7f0hb.northeurope-01.azurewebsites.net
VITE_NODE_ENV=production
# ... остальные переменные
```

### API Endpoints

Проект ожидает следующие endpoints в IMS Frontend API:

- `POST /api/auth/login` - Авторизация пользователя
- `POST /api/auth/refresh` - Обновление токена
- `POST /api/auth/logout` - Выход из системы

## 📱 Адаптивность

Страница полностью адаптивна и корректно отображается на:
- 📱 Мобильных устройствах (320px+)
- 📱 Планшетах (768px+)
- 💻 Десктопах (1024px+)

## 🎨 Дизайн

- **Цветовая схема:** Градиент от синего к фиолетовому
- **Шрифт:** Inter (Google Fonts)
- **Стиль:** Современный glassmorphism
- **Анимации:** Плавные переходы и hover-эффекты

## 🔧 Настройка

### Изменение цветов
Отредактируйте CSS переменные в `src/styles/index.css`:

```css
/* Основной градиент */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Цвет кнопки */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### Добавление новых полей
1. Обновите состояние в `LoginForm.jsx`
2. Добавьте валидацию
3. Обновите стили при необходимости

## 🚀 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📁 Структура проекта

```
src/
├── components/
│   └── LoginForm.jsx      # Компонент формы входа
├── pages/
│   ├── LoginPage.jsx      # Страница авторизации
│   └── Dashboard.jsx      # Панель управления
├── styles/
│   └── index.css          # Основные стили
├── App.jsx                # Главный компонент
└── main.jsx               # Точка входа
```

## 🎯 Особенности реализации

- **Валидация в реальном времени** - Ошибки показываются при вводе
- **Показ/скрытие пароля** - Кнопка для переключения видимости
- **Loading состояния** - Индикатор загрузки при отправке формы
- **Обработка ошибок** - Понятные сообщения об ошибках
- **Защищенные маршруты** - Автоматическое перенаправление

## 🔒 Безопасность

- Валидация на клиенте и сервере
- Защита от XSS атак
- Безопасное хранение токенов
- Валидация email формата

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

Создано с ❤️ для Partners Landing